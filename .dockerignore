# ============================================================
# Docker 镜像构建排除文件列表
# 
# 说明: 
#   这个文件指定了在构建 Docker 镜像时应该忽略的文件和目录
#   减小镜像体积，提高构建速度，避免敏感信息泄露
# ============================================================

# -------------------- Python 相关 --------------------
# Python 编译文件和缓存
__pycache__/          # Python 缓存目录
*.py[cod]             # 编译的 Python 文件
*$py.class            # 类文件
*.so                  # 共享对象文件
.Python               # Python 符号链接

# 虚拟环境目录（不需要打包到镜像中）
env/                  # 虚拟环境
venv/                 # 虚拟环境
ENV/                  # 虚拟环境
.venv                 # 虚拟环境

# -------------------- 数据文件 --------------------
# 这些文件会在容器运行时通过 volume 挂载
# 不需要打包到镜像中，避免镜像过大
# data.json           # 主数据文件（注释掉，让 Dockerfile 复制）
data/                 # 数据目录
backups/              # 备份目录
# *.json              # JSON 数据文件（注释掉，保留 data.json）

# -------------------- 日志文件 --------------------
# 日志会在容器运行时生成，不需要打包
logs/                 # 日志目录
*.log                 # 日志文件
*.pid                 # 进程 ID 文件
app.pid               # 应用进程 ID

# -------------------- IDE 和编辑器 --------------------
# 各种 IDE 的配置文件和缓存
.vscode/              # Visual Studio Code
.idea/                # IntelliJ IDEA / PyCharm
*.swp                 # Vim 交换文件
*.swo                 # Vim 交换文件
*~                    # 编辑器备份文件
.specstory/           # Cursor AI 历史记录

# -------------------- 版本控制 --------------------
# Git 相关文件不需要打包
.git/                 # Git 仓库
.gitignore            # Git 忽略规则
.gitattributes        # Git 属性

# -------------------- Docker 配置 --------------------
# Docker 相关配置文件不需要打包到镜像中
Dockerfile            # Dockerfile 本身
docker-compose.yml    # Docker Compose 配置
.dockerignore         # 本文件

# -------------------- 脚本文件 --------------------
# 管理脚本只在宿主机使用，不需要打包
manage.sh             # 统一管理脚本
*.sh                  # 所有 Shell 脚本

# -------------------- 文档文件 --------------------
# 文档文件不影响应用运行，排除以减小镜像
*.md                  # Markdown 文档
README.md             # 项目说明
CHANGELOG.md          # 变更日志
LICENSE               # 许可证文件

# -------------------- 其他文件 --------------------
.DS_Store             # macOS 系统文件
*.bak                 # 备份文件
*.tmp                 # 临时文件
*.temp                # 临时文件
.env                  # 环境变量文件（敏感信息）
.env.local            # 本地环境变量

# -------------------- 测试文件 --------------------
tests/                # 测试目录
test_*.py             # 测试文件
*_test.py             # 测试文件
pytest.ini            # Pytest 配置
.coverage             # 代码覆盖率数据

# ============================================================
# 注意事项:
# 
# 1. data.json 已注释，会在 Dockerfile 中复制到镜像
# 2. 如果有其他需要打包的文件，在此处注释掉对应的忽略规则
# 3. 敏感信息（如 .env）务必排除，避免泄露
# ============================================================

